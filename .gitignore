# Dependencies
/vendor/
/vendor-prefixed/
!/vendor-prefixed/
/vendor-prefixed/*
!/vendor-prefixed/mpdf/
/vendor-prefixed/mpdf/*
!/vendor-prefixed/mpdf/psr-http-message-shim/
/vendor-prefixed/mpdf/psr-http-message-shim/*
!/vendor-prefixed/mpdf/psr-http-message-shim/NOTICE
!/vendor-prefixed/mpdf/psr-log-aware-trait/
/vendor-prefixed/mpdf/psr-log-aware-trait/*
!/vendor-prefixed/mpdf/psr-log-aware-trait/NOTICE
/node_modules/

# Build outputs
/dist/
/build/
/coverage/

# Agent/Skills (embedded git repo)
.agent/

# Local AI-assistant scan/verification state (hundreds of MB, never ships)
.mimosa/
.omo/
.debug-journal.md
assets/icons/.mimosa/

# AI tooling — user-local settings + prompt history must never be committed.
# WordPress.org plugin-check flags these as "ai_instruction_directory" warnings
# if present in the production plugin ZIP.
.claude/
.aider*
.aider.chat.history*
.aider.input.history*
.aider.model.settings.json
.cursor/
.windsurf/
.continue/
.codeium/

# Kilo / opencode session metadata injected into .git by the CLI tools
# themselves (the user did not add these by hand). Never commit, never ship.
.git/kilo/
.git/opencode/

# IDE/Tool configs
opencode.json
.idea/
.vscode/

# Internal planning docs (keep out of repo)
docs/superpowers/
PROGRESS.md
AGENTS.md
SScribe-Plugin-Audit-GLM5-Instructions.md
SScribe-Round*-Final-*.md
SSCRIBE-COMPREHENSIVE-AUDIT-REPORT.md

# Test/debug files
test_require.php
*.test.php

# IDE & Editors
.idea/
.vscode/
*.sublime-workspace
*.sublime-project
*.swp
*.swo
*~

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# WordPress
*.log
wp-config.php
.htaccess

# Testing
.phpunit.cache/
phpunit.result.xml

# Cache
.cache/

# Temporary files
*.tmp
*.temp
*.bak
*.backup

# Environment files
.env
.env.*
!.env.example

# Alternative JavaScript package-manager lockfiles.
yarn.lock

# Generated exports (security)
/sscribe-exports/
*.zip
!vendor/**/*.zip

# Debug files
*.xdebug.log
debug.log
error_log

# OpenCode CLI
.opencode/

# Local WordPress environment
.wp-env/
/wordpress/
/wordpress-tests-lib/

# Sisyphus agent metadata
.sisyphus/

# Impeccable config
.impeccable/

# Audit reports
.audit/
AUDIT-REPORT*.md
AUDIT-*.md

# Test references (not part of plugin)
WPScan/
WordPress-Core/

# PHPStan / unit test WordPress stub (CI-only, never ships in plugin)
fake-wp/

# Real WordPress testbench scaffolded by bin/install-wp-tests.sh.
# These directories are regenerated by `composer test:wp:install` and must
# never ship in the plugin ZIP.
tests-wp/_wordpress/
tests-wp/_wordpress-tests-lib/
tests-wp/.cache/

# Audit output files (contains full local filesystem paths)
test-phpcs-output.txt

# WP Playground scratch artifacts — local browser automation logs,
# toolchain node_modules, blueprint configs, and empty temp dirs.
# Never ship in the plugin ZIP and never commit (they regenerate
# on the next Playground run).
.playwright-mcp/
.playground-tools/
playground-tools/
playground-blueprint.json

# Ad-hoc review / audit outputs (never commit, never ship).
tmp_diffs/
review-diff.patch
review-*.diff
review-*.patch
*System.Management*
