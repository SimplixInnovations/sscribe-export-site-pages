# Development files
.git/

# WordPress.org SVN assets (not in plugin ZIP)
screenshots/
.github/
.gitignore
.agent/

# Build & deploy
build/
deploy/
scripts/
dist/
WPScan/
WordPress-Core/

# Composer (dev-only — not needed in production ZIP)
composer.json
composer.lock

# Testing
tests/
phpunit.xml*
phpstan.neon*
phpstan-baseline.neon
phpcs.xml*
psalm.xml
psalm.xml.dist
.php-cs-fixer.php
.php-cs-fixer.dist.php
coverage/
.phpunit.cache/

# IDE
.idea/
.vscode/
*.sublime-workspace
*.sublime-project
*.swp
*.swo
*~

# Node
package*.json
node_modules/

# Documentation
docs/
*.md
!readme.txt

# Raw Composer vendor tree excluded from release package
vendor/

# Vendor-prefixed extras (WordPress.org requirement)
vendor-prefixed/*/.github/
vendor-prefixed/*/.git/
vendor-prefixed/*/COPYING*
vendor-prefixed/*/LICENSE*
vendor-prefixed/*/.github_changelog_generator
vendor-prefixed/*/.php-cs-fixer*
vendor-prefixed/*/phpstan.neon*
vendor-prefixed/*/phpunit.xml*
vendor-prefixed/*/tests/
vendor-prefixed/*/docs/
vendor-prefixed/*/utils/
vendor-prefixed/*/tmp/
vendor-prefixed/*/.travis.yml
vendor-prefixed/*/.scrutinizer.yml
vendor-prefixed/*/mkdocs.yml
vendor-prefixed/*/.gitattributes
vendor-prefixed/*/.gitignore
vendor-prefixed/*/README.md
vendor-prefixed/*/CHANGELOG.md
vendor-prefixed/*/CONTRIBUTING.md
vendor-prefixed/*/CREDITS.txt
vendor-prefixed/*/composer.json
vendor-prefixed/*/composer.lock
vendor-prefixed/*/ruleset.xml

# PCLZip conflict (WordPress.org requirement)
vendor-prefixed/phpoffice/phpword/src/PhpWord/Shared/PCLZip/

# Vendor-prefixed dev files (exact-name segment-level)
composer.json
.gitattributes
phpstan.neon
phpstan.neon.dist
phpstan-baseline.neon
phpunit.xml
phpunit.xml.dist
ruleset.xml
CREDITS.txt
mkdocs.yml
.travis.yml
.scrutinizer.yml
.github_changelog_generator

# Dev-only directories (segment-level — matched by build-release.php in_array check)
utils/
tmp/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Debug files
*.log
*.tmp
*.bak

# Environment
.env
.env.*

# Editor / formatter config
.editorconfig
.prettierrc
.stylelintrc.json
eslint.config.js
.opencode.json

# WordPress local dev
.wp-env.json

# Infection (mutation testing)
infection.json5
infection.json5.dist

# OpenCode config
opencode.json
.opencode/

# Husky git hooks
.husky/

# Scratch / experimental
scratch/
